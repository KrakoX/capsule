apiVersion: v1
kind: Pod
metadata:
  name: ci-privileged
  labels:
    scenario: privileged
spec:
  restartPolicy: Never
  containers:
  - name: target
    image: alpine:latest
    command: ["sleep", "3600"]
    securityContext:
      privileged: true
