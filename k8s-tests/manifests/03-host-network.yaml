apiVersion: v1
kind: Pod
metadata:
  name: ci-host-network
  labels:
    scenario: host-network
spec:
  restartPolicy: Never
  hostNetwork: true
  containers:
  - name: target
    image: alpine:latest
    command: ["sleep", "3600"]
