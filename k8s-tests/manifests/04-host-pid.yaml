apiVersion: v1
kind: Pod
metadata:
  name: ci-host-pid
  labels:
    scenario: host-pid
spec:
  restartPolicy: Never
  hostPID: true
  containers:
  - name: target
    image: alpine:latest
    command: ["sleep", "3600"]
