apiVersion: v1
kind: Pod
metadata:
  name: ci-non-root
  labels:
    scenario: non-root
spec:
  restartPolicy: Never
  containers:
  - name: target
    image: alpine:latest
    command: ["sleep", "3600"]
    securityContext:
      runAsUser: 1000
      runAsGroup: 1000
      runAsNonRoot: true
