apiVersion: v1
kind: Pod
metadata:
  name: ci-cap-sys-admin
  labels:
    scenario: cap-sys-admin
spec:
  restartPolicy: Never
  containers:
  - name: target
    image: alpine:latest
    command: ["sleep", "3600"]
    securityContext:
      capabilities:
        add: ["SYS_ADMIN"]
