version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - misspell
  settings:
    errcheck:
      check-type-assertions: true

formatters:
  enable:
    - gofmt

issues:
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
